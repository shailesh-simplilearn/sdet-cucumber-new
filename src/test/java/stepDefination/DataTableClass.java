package stepDefination;

import java.util.List;
import java.util.Map;

import cucumber.api.DataTable;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;

public class DataTableClass {

	/*
	 * @When("^I am on page$") public void i_am_on_page() throws Throwable {
	 * System.out.println("I am on page step"); }
	 * 
	 * @Then("^This page should display$") public void
	 * this_page_should_display(DataTable arg1) throws Throwable {
	 * 
	 * List<List<String>> dataList = arg1.asLists(String.class); for (List<String>
	 * listData : dataList) {
	 * System.out.println(listData.get(0)+" - "+listData.get(1)); }
	 * 
	 * 
	 * 
	 * List<Map<String, String>> mapList = arg1.asMaps(String.class, String.class);
	 * for (Map<String, String> mapData : mapList) {
	 * System.out.println(mapData.get("page")+" - "+mapData.get("author")); }
	 * 
	 * 
	 * List<List<String>> rawData = arg1.raw(); for (List<String> listData :
	 * rawData) { System.out.println(listData.get(0)+" - "+listData.get(1)); } }
	 */
}
